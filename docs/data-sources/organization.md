---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "tenzir_organization Data Source - tenzir"
subcategory: ""
description: |-
  Look up an existing organization in the Tenzir Platform. Use this to reference an organization that is managed outside of Terraform, e.g. when the provider runs with credentials scoped to that organization.
---

# tenzir_organization (Data Source)

Look up an existing organization in the Tenzir Platform. Use this to reference an organization that is managed outside of Terraform, e.g. when the provider runs with credentials scoped to that organization.

## Example Usage

```terraform
# Reference an organization that is managed outside of Terraform. This is
# the typical pattern when the provider runs with org-scoped credentials,
# which cannot create organizations.
data "tenzir_organization" "main" {
  id = "org-abc123"
}

resource "tenzir_workspace" "example" {
  name = "Security Operations"
  # The workspace is created in the organization of the authenticated
  # credential, which for org-scoped credentials is data.tenzir_organization.main.
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Unique identifier of the organization.

### Read-Only

- `icon_url` (String) URL of the organization icon.
- `name` (String) Display name of the organization.
- `require_mfa` (Boolean) Whether multi-factor authentication is required for all members.
