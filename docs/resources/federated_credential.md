---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "tenzir_federated_credential Resource - tenzir"
subcategory: ""
description: |-
  An organization-level OIDC workload identity federation credential. External OIDC tokens (e.g. issued by a CI system) whose issuer, audience, and all claim conditions match can be exchanged for Tenzir Platform keys and act as an organization member with the configured role. Requires org-admin credentials.
  The platform has no update operation for federated credentials, so changing any attribute replaces the credential. Already-minted keys stay valid until they expire.
---

# tenzir_federated_credential (Resource)

An organization-level OIDC workload identity federation credential. External OIDC tokens (e.g. issued by a CI system) whose issuer, audience, and all claim conditions match can be exchanged for Tenzir Platform keys and act as an organization member with the configured role. Requires org-admin credentials.

The platform has no update operation for federated credentials, so changing any attribute replaces the credential. Already-minted keys stay valid until they expire.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `audience` (String) Required `aud` claim value of presented tokens.
- `claim_conditions` (Map of String) Map of claim name to required exact string value; ALL conditions must match. Must contain at least one condition, since trusting an entire issuer is not supported.
- `issuer` (String) OIDC issuer URL, exactly as it appears in the tokens' `iss` claim.
- `label` (String) Human-readable label of the credential.
- `organization_id` (String) Identifier of the organization the credential belongs to.

### Optional

- `role` (String) Organization role of the federated identity, `member` or `admin`. Defaults to `member`.

### Read-Only

- `id` (String) Unique identifier of the credential.
